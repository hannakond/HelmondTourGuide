<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.helmondtourguide">

    <application
        android:allowBackup="true"
        android:icon="@drawable/flag"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AttractionsActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".HotelsActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".PubsActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".RestaurantsActivity" android:parentActivityName=".MainActivity"/>
    </application>

</manifest>